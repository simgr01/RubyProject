<% content_for :title, "Rediger opslag" %>

<div class="page-header">
  <div>
    <h1>Rediger opslag</h1>
    <p class="page-subtitle">Opdater opslaget.</p>
  </div>
  <div class="d-flex gap-2">
    <%= link_to "Se detaljer", @listing, class: "btn btn-light-outline" %>
    <%= link_to "Tilbage", listings_path, class: "btn btn-light-outline" %>
  </div>
</div>

<div class="surface-card">
  <div class="card-body">
    <%= render "form", listing: @listing %>
  </div>
</div>
