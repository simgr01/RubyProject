<div class="image-grid">
      <% @listing.images.each do |image| %>
        <div class="image-wrapper">
          <%= image_tag image.variant(resize_to_limit: [300, 300]), class: "listing-image" %>

          <%= button_to "×",
                listing_image_path(@listing, image),
                method: :delete,
                class: "delete-image-btn" %>
        </div>
      <% end %>
    </div>