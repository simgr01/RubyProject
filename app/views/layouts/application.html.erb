<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "HåndværkerBørsen" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Auction">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="app-body">
    <nav class="navbar navbar-expand-lg navbar-dark app-navbar mb-0">
      <div class="container">
        <a class="navbar-brand" href="/">HåndværkerBørsen</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto align-items-center gap-1">
            <li class="nav-item">
              <%= link_to "Opslag", listings_path, class: "nav-link" %>
            </li>
            <% if user_signed_in? && current_user.company? %>
              <li class="nav-item">
                <%= link_to "Mine bud", "#", class: "nav-link disabled", aria: { disabled: true } %>
              </li>
            <% end %>
            <% if user_signed_in? %>
              <li class="nav-item me-2 d-flex flex-column align-items-end text-white-50 small">
                <span class="fw-semibold text-white">
                  <%= current_user.display_name %>
                </span>
                <span class="text-white-50 text-uppercase" style="letter-spacing: 0.08em; font-size: 0.65rem;">
                  <%= current_user.role&.capitalize %>
                </span>
              </li>
              <li class="nav-item">
                <%= button_to "Log ud", destroy_user_session_path, method: :delete, class: "btn btn-sm btn-outline-light rounded-pill px-3 ms-1" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Log ind", new_user_session_path, class: "btn btn-sm btn-outline-light rounded-pill px-3" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <main class="page-container">
      <% if notice.present? %>
        <div class="alert alert-success flash-notice" role="alert">
          <%= notice %>
        </div>
      <% end %>

      <%= yield %>

      <footer class="site-footer text-center text-muted">
        <div>HåndværkerBørsen &middot; 2026</div>
      </footer>
    </main>
  </body>
</html>
